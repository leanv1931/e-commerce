const ItemListContainer = (props) => {
  return (
      <div className="item-list-container">
      <p className="item-list-container-hijo">{props.greeting}</p>
    </div>
  );
};

export default ItemListContainer;


